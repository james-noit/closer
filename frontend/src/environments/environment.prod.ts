// Entorno de producción
export const environment = {
  production: true,
  // en despliegue el host real deberá reemplazarse antes de compilar o via CI/CD
  apiUrl: 'https://api.closer.example.com/api'
};
